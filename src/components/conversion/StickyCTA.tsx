"use client";

import type { ConversionContext } from "@/lib/conversion-copy";

type StickyCTAProps = {
  context?: ConversionContext;
  location?: string;
  className?: string;
};

/** Design pass v2: sticky dual-CTA bar removed — floating WhatsApp only */
export function StickyCTA(_props: StickyCTAProps) {
  return null;
}
