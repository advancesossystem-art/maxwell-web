"use client";

import { homepageServices } from "@/lib/homepage";
import { getServiceIcon } from "@/lib/service-icons";
import { ArrowRight } from "@/components/ui/Icons";
import { FadeIn } from "@/components/motion/FadeIn";
import { HomeSection, HomeSectionIntro } from "@/components/home/HomeSection";
import Link from "next/link";

/** Section 4 — What we build (business solutions) */
export function ServicesExperience() {
  return (
    <HomeSection tone="deep" aria-label="What we build">
      <FadeIn>
        <HomeSectionIntro
          eyebrow="What we build"
          title="Business solutions delivered as software"
          description="Platforms, automation, and connected systems—scoped around the outcomes your leadership team needs."
        />
      </FadeIn>

      <div className="mt-14 space-y-3">
        {homepageServices.map((service, i) => {
          const Icon = getServiceIcon(service.icon);
          return (
            <FadeIn key={service.slug} delay={i * 0.04}>
              <Link href={`/services/${service.slug}`} className="home-service-link group">
                <div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between">
                  <div className="flex items-start gap-4">
                    <div className="flex h-11 w-11 shrink-0 items-center justify-center rounded-xl border border-white/[0.08] text-brand-400 transition-colors group-hover:border-brand-500/40 group-hover:text-white">
                      <Icon />
                    </div>
                    <div>
                      <h3 className="font-display text-lg font-semibold text-white sm:text-xl">
                        {service.title}
                      </h3>
                      <p className="mt-1.5 max-w-xl text-sm leading-relaxed text-muted sm:text-base">
                        {service.outcome}
                      </p>
                    </div>
                  </div>
                  <span className="inline-flex shrink-0 items-center gap-1 text-sm font-medium text-brand-500 transition-colors group-hover:text-brand-400">
                    Explore <ArrowRight />
                  </span>
                </div>
              </Link>
            </FadeIn>
          );
        })}
      </div>
    </HomeSection>
  );
}
